"""Shared constants for the Rig2 addon."""

# Custom properties that should be ignored by general UI/logic loops
INTERNAL_KEYS = {"_RNA_UI", "is_rig2"}

# Default values
IDENTITY_QUATERNION = (1.0, 0.0, 0.0, 0.0)
