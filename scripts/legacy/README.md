# Legacy Scripts

This folder keeps archived developer utilities that are not part of the
current CI/release pipeline.

- `analyze_bone_cycle.py` was moved here during codebase convergence to keep
  the main `scripts/` surface focused on active build/release tooling.
